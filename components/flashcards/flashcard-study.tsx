import React from "react";

interface Props {
  id: string;
  question: string;
  answer: string;
}

const FlashcardStudy = ({ id, question, answer }: Props) => {
  return <div></div>;
};

export default FlashcardStudy;
