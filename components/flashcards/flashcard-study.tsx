import React from "react";

interface Props {
  id: string;
  question: string;
  answer: string;
  deckId: string;
}

const FlashcardStudy = ({ id, question, answer, deckId }: Props) => {
  return <div></div>;
};

export default FlashcardStudy;
