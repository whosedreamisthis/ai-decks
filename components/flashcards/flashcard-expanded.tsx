import React from "react";

interface Props {
  id: string;
  question: string;
  answer: string;
}

const FlashcardExpanded = ({ id, question, answer }: Props) => {
  return <div></div>;
};

export default FlashcardExpanded;
